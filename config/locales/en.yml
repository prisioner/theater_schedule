en:
  activerecord:
    errors:
      models:
        show:
          attributes:
            start_date:
              order: "should be equal or before end date"
              overlap: "shouldn't overlaps with other show's periods"
            end_date:
              order: "should be equal or after start date"
              overlap: "shouldn't overlaps with other show's periods"
